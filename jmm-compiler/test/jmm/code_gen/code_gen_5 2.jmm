import static io.println(String);

class IfTest {

    public static void main(String[] args) {
        
        int n;

        n = 4;
        if(n < 2)
            io.println(args[0]);
        else
            io.println(args[1]);
    }
}