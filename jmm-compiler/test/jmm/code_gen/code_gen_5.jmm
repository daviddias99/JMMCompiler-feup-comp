import static io.println(String);

class IfTest {

    public static void main(String[] args) {
        
        int n;

        n = 0;
        if(n < 2)
            io.println(args[0]);
        else
            io.println(args[1]);
        
        if(n < 0)
            io.println(args[0]);
        else
            io.println(args[1]);
        
        if(0 < n)
            io.println(args[0]);
        else
            io.println(args[1]);
    }
}