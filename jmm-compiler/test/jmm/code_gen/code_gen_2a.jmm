import Fac.getStudentCount(int) int;
import static io.println(String);
import static io.println(int);

class EngFac extends Fac{

    public int ComputeFac(int num) {
        
        int num_aux ;
        num_aux = 3;
        
        return num_aux + num + this.getStudentCount(50);
    }

    public static void main(String[] args) {

        EngFac fac;
        // Isto é suposto dar leak??
        fac = new EngFac();
        io.println(args[0]);
        io.println(fac.ComputeFac(6));
    }
}
