import static io.println(int);

class Fac {
    public int ComputeFac(int num, int b) {
        int num_aux;
        int a;
        num = 2;
        num_aux = 0;
        num_aux = num_aux + num;
        num_aux = num_aux + num_aux * 2 + 5 / 5 - 2;
        a = 5;
        while(num_aux + 1 + 4 / 4 < 2 + 20 + b){
            num = 2;
            num = num + 2;
            num_aux = num_aux + 1;
            // while(a < 5){
            //     a = 4;
                // num_aux = num + 2 + a;
            // }
            num_aux = b +2;
            num = 2 + b;
            // a = 2;
        }

        num = num + 1;
        num_aux = num_aux + (1 + a);
        num_aux = 1;
        num_aux = num_aux + 1;

            // if(num_aux < 3)
            //     num_aux = num_aux + 1;
            // else
            //     num_aux = num_aux - 1;

        return num_aux;
    }

    public static void main(String[] args) {
        io.println(new Fac().ComputeFac(10, 5));
    }
}
