

class Fac {
    public int ComputeFac(int num) {
        int num_aux ;
        if (num < 1)
            num_aux = 1;
        else num_aux = num * (this.ComputeFac(num-1));

        return num_aux;
    }

    public static void main(String[] args) {
        
    }

    public int ComputeFac2(boolean num) {
        boolean num2;
        num = true;
        num2 = false;
        num = num && num2;

        return 3;
    }


}
