
import Bar;

class Foo extends Bar {
    int[] foo;
    boolean bar;
    int foobar;
    int len;


    public Foo method(int num, boolean bool) {
        int num_aux;
        if (num < 1) num_aux = 1;
        else num_aux = num * (this.method2());
        return new Foo();
    }

    public int method2() {
        int[] num_aux;
        foo = new int[3];
        len = 3;
        num_aux = new int[2];

        while (true && true) {
            foo[num_aux.length - 2] = 5;
        }

        return num_aux[0];
    }
}